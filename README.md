This is a Dockerfile made to create a simple way to run a cisco IOU image using real hardware. I don't provide any Cisco IP (.bin or the iourc file), so you are going to need a support contract with Cisco to use this.

Place the .bin and iourc file in the working directory. 
